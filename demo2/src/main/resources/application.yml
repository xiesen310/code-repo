spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp_high?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
logging:
  level:
    root: warn
    top.xiesen.demo2.demo2.dao: trace
  pattern: '%p%m%n'
mybatis-plus:
  global-config:
    db-config:
      delete-value:
      logic-not-delete-value: 0
      logic-delete-value: 1
