server:
  port: 8900

spring:
  application:
    name: consumer-movice

user:
  url: http://localhost:7900/user/

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:123@localhost:10000/eureka

logging:
  level:
    com.xiesen.micro.feign.FeignClient02: debug  # 给指定的 feign 设置日志输出级别，只有在 debug  的情况下才会打印日志